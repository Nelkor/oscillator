const state = {
  length: 205,
  angle: 0,
}

export const getLength = () => state.length
export const setLength = value => state.length = value

export const getAngle = () => state.angle
export const setAngle = value => state.angle = value
